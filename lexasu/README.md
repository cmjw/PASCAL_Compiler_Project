# Lexasu Simple Lexer

A very simple lexer.

Output: <token type> <token> n = <num chars>

Handles this/else/then, id type, num type, </<=/=/>/>=.

# To Build Using Make

$ make clean

$ make

$ ./lexasu

# Build Using Flex

$ bash build.sh